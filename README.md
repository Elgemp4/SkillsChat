To launch the project make the following steps : 


- Copy the .env file provided by discord into the project and adjust the database setting for your own setup
- Run `composer install`, `npm i`
- Run `npm run build` (or `npm run dev` if you run into issues)
- In an other terminal `php artisan serve`
